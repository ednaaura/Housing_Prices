# Housing_Prices
App deployment predicting housing prices.
## Run locally in Codespace

1. `pip install -r "requirements (1).txt"`
2. `streamlit run app.py --server.port 8501 --server.address 0.0.0.0`
3. In VS Code Codespaces Ports, open public URL for `8501`